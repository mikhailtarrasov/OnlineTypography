<div class="container-fluid" style="text-align: left">
    @foreach (var job in Model.Jobs)
    {
        <div class="checkbox">
            <label>
                <input type="checkbox" class="job" id="@job.Id" value="@job.JobTitle">
                @job.JobTitle
            </label>
        </div>
    }
</div>